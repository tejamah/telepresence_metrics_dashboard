# Apps

Runnable application boundaries for Embodied Presence Infrastructure.

```text
apps/
  web/        React + TypeScript experience layer
  api/        FastAPI platform API and WebSocket server
  ai-engine/  model training, evaluation, and future inference services
```

Run the web app from `apps/web` and the API from `apps/api`.
