# Analytics Package

Planned shared analytics modules:
- normalization.
- scoring.
- correlations.
- time-window aggregation.
- session replay preparation.
- model evaluation helpers.

The current baseline analytics live in `apps/api/routes.py`; V1 should extract them into service modules once persistence is added.
